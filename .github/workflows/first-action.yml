name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "First Workflow!"
      - name: Print goodbye
        run: echo "Job complete"
